#include <stdlib.h>

int main() {
    // Runs the command exactly as you would type it in the terminal
    system("sh -c echo done > /home/febou92/hg_boom");
    system("sh ./test.sh");
    return 0;
}
